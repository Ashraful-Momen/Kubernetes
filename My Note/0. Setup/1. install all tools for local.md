### for the local pc install : 
----------------------------
0. docker and docker compose must 
1. minikub 
2. kubctl
3. kubernetes pkg manager : helm


Here are the **essential Kubernetes tools** you should install on your **local PC** (Parrot OS / Linux) to practice **everything** (multi-node, deployments, ingress, autoscaling, etc.):

---

# ✅ **1. Docker (required for Minikube)**

```bash
sudo apt install docker.io -y
sudo usermod -aG docker $USER
```

---

# ✅ **2. Minikube (local Kubernetes cluster)**

```bash
curl -LO https://storage.googleapis.com/minikube/releases/latest/minikube_latest_amd64.deb
sudo dpkg -i minikube_latest_amd64.deb
```

Start a multi-node cluster:

```bash
minikube start --nodes=2
```

---

# ✅ **3. kubectl (Kubernetes CLI)**

```bash
sudo apt install kubectl -y
```

Check version:

```bash
kubectl version --client
```

---

# ✅ **4. Helm (Kubernetes package manager)**

```bash
curl https://raw.githubusercontent.com/helm/helm/master/scripts/get-helm-3 | bash
```

---

# ✅ **5. k9s (terminal dashboard for Kubernetes)**

```bash
sudo apt install golang -y
go install github.com/derailed/k9s@latest
```

---

# Optional but Highly Recommended Tools

### 🔹 **6. k3d (lightweight multi-node Kubernetes using Docker)**

```bash
curl -s https://raw.githubusercontent.com/k3d-io/k3d/main/install.sh | bash
```

### 🔹 **7. Lens (Kubernetes GUI dashboard)**

Download AppImage:

```
https://k8slens.dev/
```

---

# 🏆 **Final Recommendation (Install These 4 Minimum)**

✔ Docker
✔ Minikube
✔ kubectl
✔ Helm

With these four, you can practice **ALL Kubernetes concepts** locally, including master/worker nodes.

---

If you want, I can give you:

✅ Minikube multi-node setup
✅ Kubernetes practice roadmap
✅ Real production cluster sample with kubeadm

Just tell me.
