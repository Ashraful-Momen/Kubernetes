#start minikube: 
-----------------
>>> minikube start 

#show the all nodes - master(control-plane)/worker nodes 
-----------------------------------------
>>> kubectl get nodes 

#namespace: 
-----------
in the docker-stack.yml file we create a service for  laravel apps with nginx, phpmyadmin, node , mysql , etc
and the namespace is the same as like docker-stack . in the K8s we declear namespace, under the namespace hood
we add resource like : pods, deployment, service, config/secrets, ingress/egress etc.

#show all namespace : 
----------------------
>>> kubectl get -A namespace

or 

>>> kubectl get ns 

#create a namespace: 
---------------------
>>> kubectl create ns ostad




