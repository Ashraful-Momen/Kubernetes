Kubeadam: 
------------
make a distribue system with Kubeadam. 


# goto the aws console and create a ec2 instance with ubuntu : 1 master node, 2 worker node 

# EC2 : allow port : 
----------------------
ssh-22, 
k8s api server (kubctl)- 6443,
default TCP port for the Kubelet API in Kubernetes - 10250
k8s etc port - 2379-2380
node Port: Uses a static port in a high-numbered range (typically 30000-32767)

# Now Login every ec2 : 


# ****change hostname : 
------------------
>>> sudo hostnamectl set-hostname k8s-master
>>> bash

#*** also change the worker node hostname 


===================================== Theory about the Host Name =======================================
# Change the Host Name : 
--------------------------
Hostname is the unique name assigned to a computer/node in a network. It's like giving your computer a nickname.



------------------------------------- 
# Check current hostname
hostname
# Output: ip-172-31-45-89  (Default AWS EC2 hostname)

# Show detailed hostname info
hostnamectl status
----------------------------

BEFORE Hostname Change:                  AFTER Hostname Change:
┌─────────────────┐                     ┌─────────────────┐
│ AWS EC2 Instance│                     │ AWS EC2 Instance│
│ IP: 10.0.1.10   │                     │ IP: 10.0.1.10   │
│ Hostname:       │                     │ Hostname:       │
│ ip-10-0-1-10    │ ────rename────►     │ master-node     │
│ (Generic)       │                     │ (Descriptive)   │
└─────────────────┘                     └─────────────────┘
      ↓                                          ↓
Hard to identify                        Easy to identify role
in k8s commands                         in k8s commands

----------------------------

# Without hostname change:
$ kubectl get nodes
NAME              STATUS   ROLES    AGE   VERSION
ip-10-0-1-10      Ready    master   1h    v1.28.0
ip-10-0-1-11      Ready    <none>   1h    v1.28.0
ip-10-0-1-12      Ready    <none>   1h    v1.28.0
# 🤔 Which one is master? Which is worker?

# With hostname change:
$ kubectl get nodes
NAME           STATUS   ROLES    AGE   VERSION
master-node    Ready    control-plane   1h    v1.28.0
worker-node1   Ready    <none>          1h    v1.28.0
worker-node2   Ready    <none>          1h    v1.28.0
# ✅ Clear and understandable!

-----------------------------------------------------


===================================== End Theory about the Host Name =======================================


# After set the hostname then update system for every ec2 
>>> sudo apt update && full-upgrade -your

# update the certificate for every ec2: 
>>> sudo apt install -y apt-transport-https ca-certificates curl gnupg2 software-properties-common

===================================== Turn Off Swap Memory (Virtual Ram) =======================================
if the swap memory use then k8s unpredictable. 

# swap memory : is a virtual memory . system reserve some of memory when other apps need memory then share with them (Ram)

===================================== End Swap Memory Theory =========================================

# need some kernal packages setup for the k8s: 
----------------------------------------------

# Load kernel modules
-----------------------
sudo modprobe overlay
sudo modprobe br_netfilter

# Make modules persistent
---------------------------
cat <<EOF | sudo tee /etc/modules-load.d/k8s.conf  <------- sudo tee (means write some thing on the file.)
overlay
br_netfilter
EOF <---------- End of File means file write finish here . 


>>> sudo modprobe overlay. <------- apply the Configure on the kernal. 
>>> sudo modprobe br_netfilter. <------- apply the Configure on the kernal. 




===================================== Theory - CRT : Container Run Time As like (node js) =========================================

# Configure sysctl : Network configuration ( networking rules setup => ip_forward, ip6tables, iptables)
---------------------------------------------------------------------------------------------------------

cat <<EOF | sudo tee /etc/sysctl.d/k8s.conf
net.bridge.bridge-nf-call-iptables  = 1
net.bridge.bridge-nf-call-ip6tables = 1
net.ipv4.ip_forward                 = 1
EOF

>>>sudo sysctl --system  <----- restart systemctl 

# Disable swap permanently
sudo swapoff -a
sudo sed -i '/ swap / s/^\(.*\)$/#\1/g' /etc/fstab


-----------------------------------------------------------------------------------------------------------
#Container Run time (CRT) : k8s basicly manage container : HPA , CRUD of Container etc .that why need CRT. 

Install the container in ubuntu => containerd install in ubuntu. goto this website => https://rbsomeg.medium.com/install-containerd-in-ubuntu-20-10-for-k8s-installation-7e436e173084

here also said how to setup . 

example (***** which we already do in previous step): 
-----------

cat <<EOF | sudo tee /etc/modules-load.d/k8s.conf
overlay
br_netfilter
EOF

sudo modprobe overlay
sudo modprobe br_netfilter  

# sysctl params required by setup, params persist across reboots
cat <<EOF | sudo tee /etc/sysctl.d/k8s.conf
net.bridge.bridge-nf-call-iptables  = 1
net.bridge.bridge-nf-call-ip6tables = 1
net.ipv4.ip_forward                 = 1
EOF

# Apply sysctl params without reboot
sudo sysctl --system

===================================== End Theory - CRT : Container Run Time =========================================


#**** those setup for every ec2: 
---------------------------

#create a folder : 

>>> sudo mkdir -p /etc/apt/keyrings

# install the container run time , Add Docker GPG key and repo: 
------------------------------------------------------------------

>>>> curl -fsSL https://download.docker.com/linux/ubuntu/gpg | sudo gpg --dearmor -o /etc/apt/keyrings/docker.gpg

>>>> echo "deb [arch=$(dpkg --print-architecture) signed-by=/etc/apt/keyrings/docker.gpg] https://download.docker.com/linux/ubuntu $(lsb_release -cs) stable" | sudo tee /etc/apt/sources.list.d/docker.list > /dev/null


>>> sudo apt update


>>> sudo mkdir -p /etc/containerd

>>> containerd config default | sudo tee /etc/containerd/config.toml

# Fix cgroup driver for Kubernetes
>>> sudo sed -i 's/SystemdCgroup = false/SystemdCgroup = true/g' /etc/containerd/config.toml


>>> sudo systemctl restart containerd

>>> sudo systemctl enable containerd

>>> sudo systemctl status containerd



# Step 4: Configure kubelet to use containerd

>>> cat <<EOF | sudo tee /etc/default/kubelet
KUBELET_EXTRA_ARGS="--container-runtime=remote --container-runtime-endpoint=unix:///run/containerd/containerd.sock"
EOF

# Step 5: Install kubeadm, kubelet, kubectl

>>> sudo apt install -y apt-transport-https ca-certificates curl
>>> curl -fsSL https://packages.cloud.google.com/apt/doc/apt-key.gpg | sudo gpg --dearmor -o /etc/apt/keyrings/kubernetes-archive-keyring.gpg
>>> echo "deb [signed-by=/etc/apt/keyrings/kubernetes-archive-keyring.gpg] https://apt.kubernetes.io/ kubernetes-xenial main" | sudo tee /etc/apt/sources.list.d/kubernetes.list
>>> sudo apt update
>>> sudo apt install -y kubelet kubeadm kubectl
>>> sudo apt-mark hold kubelet kubeadm kubectl  <------ pkg not upgrade accidently 


#Make the Master note from a ec2 : 
-------------------------------------
>>> sudo kubeadm init \
> --apiserver-advertise-address=xxx.xxx.xxx.xx(Private IP) \
> --pod-network-cidr=192.168.0.0/16 --ignore-preflight-errors=NumCPU,Mem  <------- here use the pod network by user desing the ip range for k8s pods communicate.


8. Configure kubectl for your user:
--------------------------------------

>>> mkdir -p $HOME/.kube && sudo cp -i /etc/kubernetes/admin.conf $HOME/.kube/config && sudo chown $(id -u):$(id -g) $HOME/.kube/config



9. Install the Calico Network Add-on for handling the k8s master-nodes and worker-nodes network :
-------------------------------------------------------------------------------------------------

>>> kubectl create -f https://raw.githubusercontent.com/projectcalico/calico/v3.28.0/manifests/tigera-operator.yaml
>>> kubectl create -f https://raw.githubusercontent.com/projectcalico/calico/v3.28.0/manifests/custom-resources.yaml

10. Watch the master node become ready:
---------------------------------------

>>> watch kubectl get nodes -calico-system 
>>> kubectl get nodes 

For Worker node 1 and 2 (Run From the Class Notes)
---------------------------------------------------

execute the script from slave node inside
>>> ./all-node-with-user-data.sh

join the nodes to Master node (when setup the master node the we find the token): 
----------------------------------------------------------------------------------
>>> kubeadm join 172.31.0.30:6443 --token 1b84a2.08oyi0ae63vgzxwb \
> --discovery-token-ca-cert-hash sha256:aa7a18d96856fc3d7ab8c6a902ccf734cf2bc38008b7a5af0887c234f0eced9a
				

root@ip-172-31-0-30:~# kubectl get nodes

NAME             STATUS   ROLES           AGE   VERSION
ip-172-31-0-30   Ready    control-plane   76m   v1.30.14
ip-172-31-0-31   Ready    <none>          63m   v1.30.14
ip-172-31-0-32   Ready    <none>          60m   v1.30.14

root@ip-172-31-0-30:~#


Deploy a test application:
--------------------------
>>> kubectl create deployment nginx --image=sarowaralam/sarowar-nginx --replicas=3
>>> kubectl expose deployment nginx --port=80 --type=NodePort
	
Browse 
	http://<worker-node-real-ip>:<nodeport>


Assignment: 

	Automate this full process. 

-----------------------------------------------------------------------------------------------------------
-----------------------------------------------------------------------------------------------------------
-----------------------------------------------------------------------------------------------------------